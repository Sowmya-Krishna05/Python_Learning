x = -5
if x < 0:
    raise ValueError("x must be a non-negative integer")
# This code raises a ValueError if x is negative.
# The program will stop execution here if the exception is raised.
# You can catch this exception using a try-except block.
